import { TaskPopupDirective } from './task-popup.directive';

describe('TaskPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskPopupDirective();
    expect(directive).toBeTruthy();
  });
});
